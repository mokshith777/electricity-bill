#include <stdio.h>
int main()
{
  int units;
  scanf("%d", &units);
  if(units<=200){
    printf("bill is %f", 0.5*units);
  }
  else if(units<=400){
    printf("bill is %f", 0.65*units+100);
  }
    else if(units<=600){
    printf("bill is %f", 0.80*units+200);
    }
    else{
      printf("bill is %f", 1.25*units+425);
    }
return 0;
}